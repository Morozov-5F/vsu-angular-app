import { UndefinedFormatPipe } from './undefined-format.pipe';

describe('UndefinedFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UndefinedFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

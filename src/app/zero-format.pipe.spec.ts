import { ZeroFormatPipe } from './zero-format.pipe';

describe('ZeroFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
